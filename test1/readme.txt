git init

git status

git add <file name>

git rm --cached <file name>改

git commit -m "提交日志" <file name>
git log

git reset --hard 

git diff

git branch -v

git branch -d <分支名>

git branch <分支名>

git checkout <分支名>

git merge <被合并分支名>

git clone <远程库地址>

git remote -v

git remote add <别名> <远程库地址>

git remote rm <别名>

git push <别名> <分支名>

git pull <别名> <分支名>

